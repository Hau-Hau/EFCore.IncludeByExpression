name: Setup dotnet
description: Setup dotnet environment
runs:
  using: "composite"
  steps:
    - name: Setup dotnet
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: | 
          8.0.x
          7.0.x
          6.0.x
  